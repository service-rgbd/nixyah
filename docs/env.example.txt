# Environment variables (copy/paste into your `.env`)
# IMPORTANT:
# - Never commit your real secrets to git.
# - This file must contain placeholders ONLY.

## Database (Neon)
# Either name works (we map `database_url` -> `DATABASE_URL`), but prefer DATABASE_URL
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
# database_url="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

## App security tokens (SERVER-ONLY)
# Use long random values (32+ chars). Rotate if ever exposed.
ADMIN_TOKEN="change-me-admin-token-long-random"
SECRET_TOKEN="change-me-secret-token-long-random"

## Admin (optional)
# If set, a user whose email matches ADMIN_EMAIL becomes admin.
ADMIN_EMAIL="admin@example.com"
# Optional admin fallbacks (useful if your admin account doesn't have an email set yet)
# ADMIN_USERNAME="your_username"
# ADMIN_USER_ID="00000000-0000-0000-0000-000000000000"

## Support
SUPPORT_TELEGRAM_URL="https://t.me/your_support_channel_or_group"

## Telegram bot (optional, for commandes produits adultes)
TELEGRAM_BOT_TOKEN="123456:replace-me"
TELEGRAM_CHAT_ID="123456789"

## Cloudflare R2 (S3-compatible uploads) (SERVER-ONLY)
R2_ACCOUNT_ID="replace-me"
R2_ACCESS_KEY_ID="replace-me"
R2_SECRET_ACCESS_KEY="replace-me"
R2_BUCKET="replace-me"
# Optional: for public URLs or custom domain (if you serve files publicly)
R2_PUBLIC_BASE_URL="https://YOUR_PUBLIC_BUCKET_DOMAIN"
# Optional: if you want to override endpoint (default derived from account id)
R2_ENDPOINT="https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com"

## Resend (emails transactionnels : confirmation + mot de passe oublié) (SERVER-ONLY)
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxx"
# Expéditeur utilisé pour les emails (doit être un domaine vérifié chez Resend)
RESEND_FROM="NIXYAH <no-reply@nixyah.com>"

## URL publique de l’app (utilisée dans les liens des emails)
APP_BASE_URL="http://localhost:5000"

## Google OAuth (SERVER-ONLY secret; client id is public)
GOOGLE_CLIENT_ID="replace-me.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="replace-me"
GOOGLE_REDIRECT_URI="http://localhost:5000/api/auth/google/callback"

## Cloudflare Turnstile
TURNSTILE_SECRET_KEY="replace-me"          # SERVER-ONLY
VITE_TURNSTILE_SITE_KEY="replace-me"      # PUBLIC (frontend)

# Environment variables (copy/paste into your `.env`)
# Note: `.env` files are ignored in this repo / IDE, so this file is the reference.

## Database (Neon)
# Either name works (we map `database_url` -> `DATABASE_URL`), but prefer DATABASE_URL
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
# database_url="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

## App security tokens
ADMIN_TOKEN="ra.fils27@hotmail.com"
SECRET_TOKEN="80cc8fe9f356772ed58c290cae62ec332bfcacec4ab3faceaca4284d3ed4696d"

## Admin (optional)
# If set, a user whose email matches ADMIN_EMAIL (or username == ADMIN_EMAIL as fallback) becomes admin.
ADMIN_EMAIL="Ra.fils27@hotmail.com"
SUPPORT_TELEGRAM_URL="https://t.me/+cNj_edHZTyc2YWE0"

## Telegram bot (optional, for commandes produits adultes)
# TOKEN de ton bot Telegram (BotFather)
TELEGRAM_BOT_TOKEN=8302790478:AAG2__tWbUN6q75i8o2fXyWk3I740umgLAg
# ID du chat ou du channel où recevoir les commandes (numérique)
TELEGRAM_CHAT_ID=8302790478

## Cloudflare R2 (S3-compatible uploads)
R2_ACCOUNT_ID=90b110d9c9e7d9671f85ac48fe800e61
R2_ACCESS_KEY_ID=8f8278f34a64ceb56f8b9d3b45de513b
R2_SECRET_ACCESS_KEY=74c86fcec97b68c0d86e1832d27087a42da7f407b06cf2cea513f595de6d74af
R2_BUCKET=djantrah
# Optional: for public URLs or custom domain (if you serve files publicly)
R2_PUBLIC_BASE_URL=https://pub-c4d2f077b8bf4164a9ecdea6a7d140c1.r2.dev
# Optional: if you want to override endpoint (default derived from account id)
R2_ENDPOINT=https://90b110d9c9e7d9671f85ac48fe800e61.r2.cloudflarestorage.com

## Resend (emails transactionnels : confirmation + mot de passe oublié)
# Clé API secrète Resend
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxx"
# Expéditeur utilisé pour les emails (doit être un domaine vérifié chez Resend)
RESEND_FROM="NIXYAH <no-reply@nixyah.com>"

## Authentification Google OAuth2 (connexion via Google)
# Identifiant client OAuth Google (type "Application Web")
GOOGLE_CLIENT_ID="xxxxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com"
# Secret client associé
GOOGLE_CLIENT_SECRET="xxxxxxxxxxxxxxxxxxxxxxxx"
# URL de redirection configurée dans la console Google (ex: https://api.nixyah.com/api/auth/google/callback)
GOOGLE_REDIRECT_URI="https://api.nixyah.com/api/auth/google/callback"

## Cloudflare Turnstile (anti-bot)
# Côté FRONT (Vite) : VITE_TURNSTILE_SITE_KEY="0x4AAAAAA...."  (à définir dans ton env de build)
# Côté BACK (Render/API) :
TURNSTILE_SECRET_KEY="0x4AAAAAA...."

## URL publique de l’app (utilisée dans les liens des emails)
# En développement, laisse par défaut ou adapte selon ton port Vite
APP_BASE_URL="http://localhost:5000"

# En production (frontend Cloudflare)
# APP_BASE_URL="https://nixyah.com"



